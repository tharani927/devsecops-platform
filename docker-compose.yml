services:

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    ports:
      - "3000:80"
    depends_on:
      - api
    networks:
      - devsecops-network

  api:
    build:
      context: ./backend
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    environment:
      DATABASE_URL: postgresql://devsecops:devsecops123@db:5432/devsecops
    depends_on:
      db:
        condition: service_healthy
    networks:
      - devsecops-network

  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: devsecops
      POSTGRES_PASSWORD: devsecops123
      POSTGRES_DB: devsecops
    ports:
      - "5433:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U devsecops -d devsecops"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - devsecops-network

networks:
  devsecops-network:
    driver: bridge

volumes:
  postgres-data: